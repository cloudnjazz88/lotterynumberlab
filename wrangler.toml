# Static site for Cloudflare Workers + Assets (npx wrangler deploy).
# Do not set `main` — there is no Worker entry file; src/ is browser JavaScript.
name = "lotterynumberlab"
compatibility_date = "2026-08-24"

[assets]
directory = "./dist"
